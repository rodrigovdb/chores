<%- resource_class.omniauth_providers.each do |provider| %>
  <%# <%= button_to "#{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), data: { turbo: false }, class: 'btn btn-outline-secondary' %1><br /> %>
  <%= button_to "Login with Keycloak", omniauth_authorize_path(resource_name, provider), data: { turbo: false }, class: 'btn btn-outline-dark' %><br />
<% end %>
