<h1><%= @kid.name %></h1>

<div class="card">
  <div class="card-body">
    <h5 class="card-title">
      <%= I18n.localize(@week.begin) %>
      -
      <%= I18n.localize(@week.end) %>
    </h5>

    <%= turbo_frame_tag 'week' do %>
      <%= render 'chores_table', week: @week, kid: @kid %>
    <% end %>
  </div>
</div>
